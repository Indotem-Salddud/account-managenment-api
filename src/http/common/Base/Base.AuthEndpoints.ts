/**
 * ! Auth endpoints
 * * whitehatdevv - 2022/01/12
 */
export enum AuthEndpoints {
    Login = '/login',
    UpdatePassword = '/password',
    Onboarding = '/onboarding',
    Refresh = '/refresh',
}